# RiverSpawner

## 1. 개요 (Overview)
- **위치**: `Assets/Scripts/Gameplay/RiverSpawner.cs`
- **역할**: 물수제비 인게임(장거리/타겟 모드) 및 청크 릴레이 시, 물 표면에 부스트 패드, 바위 장애물, 튀어오르는 물고기, 친구 깃발, 연잎/연꽃 군락 등을 절차적으로 동적 스폰하는 게임플레이 스포너.

## 2. 불변 원칙 (Immutable Principles)
- **실시간 수면 Bounds 참조 (하드코딩 금지)**: 수면의 `BoxCollider`로부터 가로폭(`minX`, `maxX`), 세로길이(`minZ`, `maxZ`), 수면 높이(`waterY`)를 동적으로 획득하여 실제 물 영역 내에서만 스폰.
- **땅속 스폰 방지**: `IsValidWaterPosition` 레이캐스트를 통해 지형이 수면보다 높이 솟은 땅 위 스폰 차단.
- **모드별 격리**: `LongDistance`와 `TargetAccuracy` 모드별로 적합한 엔티티 패턴 유지.

## 3. 변경 이력 (Changelog)
- ### [2026-09-04] 🌀 리듬 아케이드 모드 부스트 패드 잔류 차단 & 랜덤 링/부스트 패드 유기적 지그재그 분산 배치
  - **수정 목적**: 
    1. 청크 스트리밍 릴레이 중 특정 상황에서 부스트 패드가 링 대신 섞여 생성되던 현상 원천 차단.
    2. 동일 Z 계산 지점에서 강을 가로질러 일직선 행렬로 스폰되던 부자연스러움을 해소하고 강줄기를 따라 유기적으로 앞뒤 흩뿌려진 지그재그 배치 적용.
  - **핵심 구조**:
    - `TrySpawnBoostPad`: `GameController.Instance.currentMode` SSOT 실시간 동기화로 캐쥬얼(RhythmArcade) 모드 시 어떠한 청크에서도 100% `RandomRing` 생성 보장.
    - 강폭별(협곡/중형/대형) 및 분기 물길 스폰 시 각 오브젝트마다 독립된 거리 오프셋($\pm 4\text{m} \sim 10\text{m}$)을 부여하고, `GlobalRiverPath.EvaluateAtDistance`로 곡선 물길 흐름과 접선 각도를 각각 개별 평가하여 유기적 지그재그 배치 완성.
  - **컴파일 검증**: 0 Errors 통과.
- ### [2026-09-04] 🌊 곡선 맵(급커브/U턴) 및 섬 분기 물길 완벽 스폰 & 다중 청크 스트리밍 연동
  - **수정 목적**: 2번째 청크 및 급커브/섬 분기 지형에서 Z축 단순 필터 오차 및 물리 엔진 미동기화로 인해 수면 오브젝트가 누락되던 결함 완전 해소.
  - **핵심 구조**:
    - `GlobalRiverPath`: 청크 실제 도킹 순서(`DynamicChunks`) 기반 스플라인 체인 결합 및 청크별 거리(`GetSegmentDistanceRange`) 100% 정밀 매칭.
    - `RiverSpawner`:
      - `Physics.SyncTransforms()`를 통해 청크 생성 즉시 물리 콜라이더 트리 강제 동기화.
      - 베이킹된 강줄기 곡선 궤적을 따라 스폰 시 무의미한 직선 $Z$축 필터를 면제(`checkZBounds: false`)하여 90도 회전/곡선 맵에서도 100% 정상 스폰.
      - `DetectSplitWaterChannels` 스캔 반경 75m 확장으로 베이킹되지 않은 섬 반대편 물길까지 완벽 포착하여 양쪽 물길 모두 균등 스폰.
      - 게임 시작 시 현재 로드된 모든 청크(0번, 1번...)를 순회하여 시작부터 전 구간 수면 오브젝트 생성 보장.
  - **컴파일 검증**: 0 Errors, 0 Warnings 통과.
- ### [2026-08-30] 섬(Island) 분기 지형 대응 다중 수면 채널(Split Channels) 동시 스폰 시스템 탑재
  - **수정 목적**: 중간에 섬이나 모래톱으로 인해 물길이 좌/우 2개로 갈라지는 지형에서, 플레이어가 어느 쪽 물길을 선택하더라도 양쪽 물길 모두에서 부스트 패드, 바위, 물고기가 균등하고 안전하게 스폰되도록 지원.
  - **핵심 구조**:
    - `DetectSplitWaterChannels`: 단면에서 섬으로 분리된 2개 이상의 독립 수면 채널(Water Channels)을 실시간 검출.
    - 분기 물길 감지 시 각 갈래길마다 부스트 패드, 바위, 물고기를 1개씩 분산 스폰하여 양방향 플레이 경험 보장.
    - 반경 겹침 방지(`HasNearbySpawnedEntity`)와 강폭 비례 적응형 밀도 제어 유지.
  - **컴파일 검증**: 0 Errors.
- **2026-08-30**: 강폭(Width) 비례 적응형 밀도 제어 및 반경 겹침 방지 시스템 구축
  - **수정 목적**: 좁은 협곡 구간에서 다수의 오브젝트가 겹치거나 통로를 가로막는 문제를 원천 차단.
  - **핵심 구조**:
    - 강폭 < 15m (협곡): 부스트 패드/물고기 1개만 중앙 스폰, 바위는 측면 1개만 확률 스폰하여 최소 50% 이상의 안전 탈출 통로 확보.
    - 강폭 15m ~ 25m: 1~2개 분산 스폰.
    - 강폭 >= 25m: 2~3개 좌/중/우 분산 스폰.
    - `HasNearbySpawnedEntity`: 반경 3.8m~4.2m 이내에 이미 다른 스폰 엔티티가 존재하면 겹침 방지(스킵).
  - **컴파일 검증**: 0 Warnings, 0 Errors.
- **2026-08-28**: 
  - `GetWaterColliderBounds`에 `minZ`, `maxZ` 반환 추가.
  - 고정 길이 대신 슬롯 맵 청크(500m 등 가변 지형)의 `autoChunkSize`를 실측하여 해당 1개 청크 지형 경계(`startZ + 20m ~ endZ - 20m`) 내에서만 엔티티 스폰.
  - `IsValidWaterPosition`을 전면 개편: 초고도 `RaycastAll`로 메쉬 지형(`MeshCollider`), 터레인 지형(`TerrainCollider`), 수면 콜라이더를 전수 감지하고 안전 수심(`waterDepth >= 0.35m`)을 확보하여 땅속 파묻힘 및 허공 스폰 100% 원천 차단.
